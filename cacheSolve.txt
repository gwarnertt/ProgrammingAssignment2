cacheSolve<- function(y,...){
    inverse <- y$getInverse()
    if(!is.null(inverse)){
    message("getting inverse")
    return(inverse)
}
data <- y$getMatrix()
inverse <- solve(data,...)
y$cacheInverse(inverse)
inverse
}
